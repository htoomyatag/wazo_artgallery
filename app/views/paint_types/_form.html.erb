
<%= form_for @paint_type, :html => { :class =>"form-horizontal" } do |f| %>
  <% if @paint_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@paint_type.errors.count, "error") %> prohibited this paint_type from being saved:</h2>

      <ul>
      <% @paint_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="control-group">
    <label class="control-label" for="inputEmail">Types</label>
        <div class="controls">
    <%= f.text_field :type_name %>
  </div>
   </div>
   <div class="control-group">
        <div class="controls">
      <%= f.submit  :class => "btn" %>
  </div>
  </div>
<% end %>
